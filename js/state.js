import { CONFIG, KEY, ABIL_BY_K, MUTS, TREE_SKINS } from './config.js';
import { safeInt } from './utils.js';

export function maxHpOf(o){ /* … */ }
export function load(){ /* …возвращает объект S… */ }
export const S = load();
export function save(){ /* … */ }
